streamlit>=1.22

# LangChain core and integrations
langchain>=0.1.0       # or latest stable
langchain-core
langchain-community
langchain-ollama

# Environment and HTTP
python-dotenv
requests

# Optional: tokenization / embeddings / HF models (if you use them)
tiktoken
transformers
faiss-cpu

# Optional: direct Ollama Python client (if you want it)
ollama
